# ---- 密钥与本地配置 ----
# **真正的那一份不在这个仓库里，也不在任何工作区里** —— 它在 `~/.tudouni/config.json`
# （首次运行自动生成，权限 0600）。理由见 agent_runtime/userconfig.py：换个项目干活
# 不该换密钥，而装成命令之后包在 site-packages 里，那不是能放配置的地方。
#
# 所以这一节现在守的只是"别把误放在仓库里的密钥带进去"。模板本身
# （agent_runtime/config.example.json）是**要提交**的，它里面没有真值，有测试盯着。
.env
.env.*
# 旧位置（`.env` / `models.local.json`）程序已经不再读。留着这几行是因为它们可能还躺在
# 磁盘上、而且装的就是密钥 —— 绝不该被 `git add -A` 顺手带进去。
models.local.json
models.json
config.local.json
*.token
.ericai_debug_token.txt

# ---- 运行期数据：整个 .tudouni/ ----
# 这是**每个工作区一份的运行时私有目录**，里面装的全是本机数据：
#
#   .tudouni/permissions.json   权限策略（按一次 t 程序就会改它）
#   .tudouni/sessions/          会话状态（我们实测过一次会话 71% 的体积是工具读到的文件正文）
#   .tudouni/logs/              审计轨迹（含工具参数预览，write_file 的 content 可能敏感）
#   .tudouni/skills/            技能（模型按需读取的步骤文件）
#
# **技能也在里面，所以它同样不提交。** 这是工作区的私有配置，不是源码 —— 想把它变成
# 随仓库走的团队策略，删掉下面这一行即可；那时候要一起想清楚的是"谁来 review 这些技能"，
# 而不是"忽略哪几行"。
.tudouni/
# 旧位置（合并进 .tudouni/ 之前）。程序已经不再读写它们，但留着这行是因为它们可能还在
# 磁盘上，而里面的东西和上面同一类，绝不该被 `git add -A` 顺手带进去。
.sessions/
.logs/
.tudouni.json
.tudouni.json.tmp

# ---- 虚拟环境与缓存 ----
.venv/
.uv-cache/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
pytest-cache-files-*/
tests/_tmp/
.pytest-tmp/
# 性能分析脚本的临时工作区（scripts/perf_*.py 建的假工作区、假会话、假日志）
.perf-tmp/
# 临时草稿区（一次性脚本、调试输出）。**它不是"稍后会清掉"的借口** ——
# 值得留下的东西进 scripts/，其余的用完就删。
.tmpctx/

# ---- 出发布包（scripts/build_release.py）----
# dist/ 里是冻结好的可执行文件，几十兆且每个平台一份 —— 它是**产物**，不是源码。
# build/ 是 PyInstaller 的中间目录，比 dist/ 还大。两者都由那一条命令重建。
dist/
build/

# ---- 编辑器与系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini
