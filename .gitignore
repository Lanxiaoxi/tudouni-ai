# ---- 密钥与本地配置 ----
# 绝不提交。源码进公开仓库，密钥不进。
.env
.env.*
*.token
.ericai_debug_token.txt

# ---- 运行期数据 ----
# 会话文件里有工具读到的**文件正文**（我们实测过一次会话 71% 的体积是工具输出）；
# 审计日志里有工具**参数预览**（write_file 的 content 可能含敏感内容）。
# 两者都是本机产生的数据，不该进版本库。
.sessions/
.logs/

# ---- 虚拟环境与缓存 ----
.venv/
.uv-cache/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
pytest-cache-files-*/
tests/_tmp/
.pytest-tmp/

# ---- 编辑器与系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini
