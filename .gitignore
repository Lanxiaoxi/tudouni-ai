# ---- 密钥与本地配置 ----
# 绝不提交。源码进公开仓库，密钥不进。
.env
.env.*
# 但模板要提交 —— 它是给人看的那一份，里面没有真密钥。
# 注意顺序：gitignore 后出现的 ! 才能把上面已经排除的文件重新包含进来。
!.env.example
*.token
.ericai_debug_token.txt

# ---- 运行期数据 ----
# 会话文件里有工具读到的**文件正文**（我们实测过一次会话 71% 的体积是工具输出）；
# 审计日志里有工具**参数预览**（write_file 的 content 可能含敏感内容）。
# 两者都是本机产生的数据，不该进版本库。
.sessions/
.logs/

# 权限策略是**工作区本地配置**：它记的是"这个工作区信任什么"，而且按一次 t 程序
# 就会往里写 —— 和 .env 同一类，不是源码。想把它固定成团队策略，删掉下面两行即可。
.tudouni.json
# 写入走临时文件 + os.replace，崩溃可能留下半份；它同样不该进版本库。
.tudouni.json.tmp

# ---- 虚拟环境与缓存 ----
.venv/
.uv-cache/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
pytest-cache-files-*/
tests/_tmp/
.pytest-tmp/

# ---- 编辑器与系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini
