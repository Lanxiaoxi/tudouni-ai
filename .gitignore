# ---- 密钥与本地配置 ----
# 绝不提交。源码进公开仓库，密钥不进。
.env
.env.*
# 但模板要提交 —— 它是给人看的那一份，里面没有真密钥。
# 注意顺序：gitignore 后出现的 ! 才能把上面已经排除的文件重新包含进来。
!.env.example
# 模型路由配置：**真的那一份不进仓库**（它里面有 base_url、也可能有密钥），模板照旧。
# 找文件的顺序见 state/catalog.py（工作区这份 → 用户级 ~/.tudouni/models.json → 内置）。
models.local.json
!models.example.json
*.token
.ericai_debug_token.txt

# ---- 运行期数据：整个 .tudouni/ ----
# 这是**每个工作区一份的运行时私有目录**，里面装的全是本机数据：
#
#   .tudouni/permissions.json   权限策略（按一次 t 程序就会改它）
#   .tudouni/sessions/          会话状态（我们实测过一次会话 71% 的体积是工具读到的文件正文）
#   .tudouni/logs/              审计轨迹（含工具参数预览，write_file 的 content 可能敏感）
#   .tudouni/skills/            技能（模型按需读取的步骤文件）
#
# **技能也在里面，所以它同样不提交。** 这是工作区的私有配置，不是源码 —— 想把它变成
# 随仓库走的团队策略，删掉下面这一行即可；那时候要一起想清楚的是"谁来 review 这些技能"，
# 而不是"忽略哪几行"。
.tudouni/
# 旧位置（合并进 .tudouni/ 之前）。程序已经不再读写它们，但留着这行是因为它们可能还在
# 磁盘上，而里面的东西和上面同一类，绝不该被 `git add -A` 顺手带进去。
.sessions/
.logs/
.tudouni.json
.tudouni.json.tmp

# ---- 虚拟环境与缓存 ----
.venv/
.uv-cache/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
pytest-cache-files-*/
tests/_tmp/
.pytest-tmp/

# ---- 编辑器与系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini
