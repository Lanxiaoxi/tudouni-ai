# 复制这个文件为 .env，然后填上你的密钥：
#
#     Copy-Item .env.example .env
#
# .env 已经被 .gitignore 忽略，不会被提交。
# 这个模板本身没有密钥，所以它是被提交的 —— 它的作用就是告诉你该填什么。

DEEPSEEK_API_KEY=

# 可选：换网关或换模型。留空就用默认值。
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-flash

# 可选：联网搜索（web_search 工具）。不填就不注册这个工具，fetch_web 不受影响。
# 到 https://app.tavily.com 拿一个 key，形如 tvly-...
# TAVILY_API_KEY=
# 换网关时用这个（默认 https://api.tavily.com）：
# TAVILY_BASE_URL=https://api.tavily.com

# 注意：真实环境变量的优先级高于 .env。
# 也就是说，如果你同时 setx 了 DEEPSEEK_API_KEY 又写了 .env，以环境变量为准。
# TAVILY_API_KEY 同理。
